import { ArrowRight, CheckCircle2, LockKeyhole, ShieldCheck } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { BrandMark } from '../components/BrandMark';
import { login } from '../services/api';

export function LoginPage() {
  const navigate = useNavigate();
  const [email, setEmail] = useState('anika.menon@clinic.org');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setError(''); try { await login(email, password); navigate('/'); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to sign in'); } finally { setLoading(false); } }
  return <div className="flex min-h-screen bg-white"><div className="hidden w-[46%] flex-col justify-between overflow-hidden bg-ink p-10 text-white lg:flex"><BrandMark dark /><div className="relative"><div className="absolute -left-24 -top-24 h-96 w-96 rounded-full border border-teal-400/10" /><div className="absolute -left-10 -top-10 h-64 w-64 rounded-full border border-teal-400/10" /><p className="eyebrow !text-teal-300">The care intelligence layer</p><h1 className="section-title mt-5 max-w-md text-5xl font-extrabold leading-[1.1]">See clearly.<br /><span className="text-teal-300">Screen early.</span><br />Save sight.</h1><p className="mt-6 max-w-sm text-sm leading-6 text-slate-300">A self-checking AI workspace designed to help primary care teams make every retinal screening more explainable and actionable.</p><div className="mt-10 space-y-3 text-sm text-slate-300"><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-teal-300" /> Quality-first image intake</p><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-teal-300" /> Evidence-backed results</p><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-teal-300" /> Human review when it matters</p></div></div><p className="text-[11px] text-slate-500">RETINA-NEXUS · Prototype workspace · v0.1.0</p></div><div className="flex flex-1 items-center justify-center bg-mist px-6 py-10 sm:px-12"><div className="w-full max-w-[390px]"><div className="lg:hidden"><BrandMark /></div><div className="mt-12 lg:mt-0"><p className="eyebrow">Secure workspace</p><h2 className="section-title mt-3 text-3xl font-extrabold">Sign in to continue</h2><p className="mt-3 text-sm leading-6 text-slate-500">Access screening, evidence verification, and clinician review tools.</p></div><form onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-xs font-bold text-ink">Work email</span><input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="w-full rounded-xl border border-line bg-white px-4 py-3 text-sm outline-none transition focus:border-teal-400 focus:ring-4 focus:ring-teal-50" placeholder="you@clinic.org" /></label><label className="block"><span className="mb-2 block text-xs font-bold text-ink">Password</span><div className="relative"><input required minLength={8} type="password" value={password} onChange={(event) => setPassword(event.target.value)} className="w-full rounded-xl border border-line bg-white px-4 py-3 text-sm outline-none transition focus:border-teal-400 focus:ring-4 focus:ring-teal-50" placeholder="Enter your password" /><LockKeyhole size={16} className="absolute right-4 top-3.5 text-slate-300" /></div></label>{error && <div role="alert" className="rounded-xl border border-rose-200 bg-rose-50 p-3 text-xs leading-5 text-rose-800">{error}</div>}<div className="flex items-center justify-between text-xs"><label className="flex items-center gap-2 text-slate-500"><input type="checkbox" className="rounded border-slate-300 text-teal-600 focus:ring-teal-200" /> Remember me</label><button type="button" className="font-bold text-teal-700 hover:text-teal-600">Forgot password?</button></div><button disabled={loading} className="btn-primary w-full py-3 disabled:opacity-60">{loading ? 'Signing in…' : 'Sign in to workspace'} <ArrowRight size={16} /></button></form><div className="mt-8 flex items-start gap-3 rounded-xl border border-teal-100 bg-teal-50 p-3 text-xs leading-5 text-teal-800"><ShieldCheck size={16} className="mt-0.5 shrink-0 text-teal-600" /><span>Access is protected by workspace roles and audit logging. This prototype does not claim regulatory certification.</span></div></div></div></div>;
}
