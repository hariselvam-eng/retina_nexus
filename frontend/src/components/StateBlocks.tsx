import { AlertTriangle, Inbox, LoaderCircle } from 'lucide-react';

export function LoadingState({ label = 'Loading workspace' }: { label?: string }) { return <div className="card flex min-h-48 flex-col items-center justify-center text-center"><LoaderCircle className="animate-spin text-teal-600" size={26} /><p className="mt-3 text-sm font-bold text-ink">{label}</p></div>; }
export function ErrorState({ message = 'Something went wrong' }: { message?: string }) { return <div className="card flex min-h-48 flex-col items-center justify-center text-center"><AlertTriangle className="text-amber-500" size={26} /><p className="mt-3 text-sm font-bold text-ink">{message}</p><p className="mt-1 text-xs text-slate-400">Try again or contact your workspace administrator.</p></div>; }
export function EmptyState({ title, message }: { title: string; message: string }) { return <div className="card flex min-h-64 flex-col items-center justify-center px-6 text-center"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-mist text-slate-400"><Inbox size={22} /></div><p className="mt-4 text-sm font-bold text-ink">{title}</p><p className="mt-1 max-w-sm text-xs leading-5 text-slate-400">{message}</p></div>; }
