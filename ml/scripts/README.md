# Dataset governance commands

The runnable commands live in the repository-level `scripts/` directory so they can be called exactly as `python scripts/validate_dataset.py --dataset aptos2019`. This directory is kept as the ML-facing documentation boundary for future training orchestration.
