# RETINA-NEXUS ML workspace

This workspace is reserved for reproducible dataset manifests, training runs, evaluation reports, and model weights. Model adapters in backend/app/ml expose registered versions through the service boundary before they are enabled in the API. See docs/classification.md for the DR training and benchmark workflow. No model weights or datasets are committed.
